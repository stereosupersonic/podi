#!/usr/bin/env ruby

ENV["BUNDLE_GEMFILE"] ||= File.expand_path("../Gemfile", __dir__)

require "bundler/setup"
require "sidekiq/cli"

cli = Sidekiq::CLI.instance
cli.parse(ARGV.empty? ? ["-C", "config/sidekiq.yml"] : ARGV)
cli.run
